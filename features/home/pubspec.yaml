name: home

environment:
  sdk: ^3.5.4
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  core:
    path: ../../core

  core_ui:
    path: ../../core_ui

  domain:
    path: ../../domain

  navigation:
    path: ../../navigation

  settings:
    path: ../settings

  character:
    path: ../character

  curved_navigation_bar: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner: ^2.4.8
  auto_route_generator: ^9.0.0